# project2
Library Management System Backend is a RESTful API built with Node.js, Express.js, MongoDB, and Mongoose using the MVC architecture. It features JWT-based authentication, bcrypt password encryption, role-based access (Admin/User), and CRUD operations for managing books. APIs were tested using Thunder Client.
